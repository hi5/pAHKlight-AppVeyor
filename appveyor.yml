version: 1.0.{build}

install: 
  - cinst autohotkey.install

build: off

before_test:
  - cmd: cd test

#test_script: 
#  - test.cmd

# you can call ahk script it seems
test_script: 
  - ValidateScript.ahk

after_test:
  - cmd: echo testoutput.txt
